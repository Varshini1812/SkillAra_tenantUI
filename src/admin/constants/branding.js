/**
 * Fallback branding for a tenant that has not set its own colour yet — the
 * SkillAra product primary. Keep this in step with `--color-brand` in index.css.
 */
export const DEFAULT_TENANT_BRAND = "#0F766E";
