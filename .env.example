# Wildcard domain for tenant subdomains (acme.skillara.com).
# Leave empty until a real wildcard domain is live: the app then finds the
# workspace from the sign-in email and keeps it out of the URL entirely.
VITE_ROOT_DOMAIN=
VITE_API_URL=
